Update the QueryAI backend prompt engineering and explanation system so that Beginner, Intermediate, and Expert explanation modes produce clearly different outputs.

CURRENT ISSUE:
All explanation levels are returning nearly identical responses. The AI is not strongly adapting explanation depth or terminology.

TASK:
Refactor the optimize route and AI prompting logic to enforce explanation-level behavior properly.

REQUIREMENTS:

1. FRONTEND UPDATE

* Add `explanation_level` to optimize API request body.
* Values:

  * beginner
  * intermediate
  * expert

Example request:

```json
{
  "query": "...",
  "db_type": "MySQL",
  "explanation_level": "expert"
}
```

2. BACKEND UPDATE
   Modify `/api/optimize` route to:

* Accept explanation_level
* Inject explanation rules into system prompt dynamically
* Ensure the AI explanation style changes significantly

3. REPLACE CURRENT SYSTEM PROMPT WITH THIS:

"You are an elite database performance engineer specializing in SQL query optimization.

Your job:

1. Analyze SQL queries
2. Detect bottlenecks
3. Identify risks
4. Optimize performance
5. Explain reasoning based on explanation level

The user provides:

* SQL query
* database type
* explanation level

IMPORTANT:
You MUST adapt your explanation style STRICTLY according to explanation_level.

EXPLANATION RULES:

BEGINNER MODE:

* Explain like teaching a student new to databases
* Use simple words
* Avoid advanced database terminology
* Explain WHY things are slow in plain English
* Use analogies when helpful
* Friendly educational tone

INTERMEDIATE MODE:

* Use developer-friendly technical language
* Mention indexes, joins, scans, sorting, filtering
* Explain optimization decisions clearly
* Suitable for software engineers

EXPERT MODE:

* Use advanced DBA and query-planner terminology
* Mention:

  * sequential scans
  * index selectivity
  * cardinality estimation
  * execution plan inefficiencies
  * I/O overhead
  * optimizer behavior
  * predicate pushdown
  * index utilization
* Keep explanations concise but highly technical

You MUST make each explanation level noticeably different.

Return ONLY valid JSON.

Required JSON structure:
{
"optimized_query": "string",
"issues_found": ["string"],
"detected_risks": ["string"],
"index_suggestions": ["string"],
"performance_gain": "string",
"complexity_score": "string",
"query_risk_level": "Low | Medium | High | Critical",
"estimated_execution_time_before": "string",
"estimated_execution_time_after": "string",
"explanation": "string"
}"

4. ADD VALIDATION

* Default explanation level to "intermediate" if missing
* Reject invalid explanation levels

5. UI IMPROVEMENT

* Add explanation level badge in results panel
* Show different colors:

  * Beginner → green
  * Intermediate → blue
  * Expert → red

6. TESTING
   Test all 3 levels using the same SQL query and confirm:

* Beginner uses simple educational language
* Intermediate uses moderate technical explanation
* Expert uses advanced DBA terminology

7. GOAL
   The explanations should feel like:

* Beginner → SQL teacher
* Intermediate → software engineer
* Expert → senior database administrator

Maintain all existing functionality and preserve clean architecture.
